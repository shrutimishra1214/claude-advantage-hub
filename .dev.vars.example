# Copy to .dev.vars for `npm run cf:dev` / `wrangler dev` (gitignored).
# These map to process.env in the Worker during local Cloudflare emulation.

SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_PUBLISHABLE_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
